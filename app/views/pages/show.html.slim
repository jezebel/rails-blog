article.post
  header.post-header
    h1.post-title
      = @page.title
      
    - if @page.image
      .post-image
        .image-wrapper
          = image_tag @page.image
      - if @page.caption
        .image-text
          = @page.caption
  
  .post-content
    = @page.html
